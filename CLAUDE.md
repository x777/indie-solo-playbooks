# CLAUDE.md

Follow `AGENTS.md`.

When the user asks to launch, market, pick a channel, or apply a founder playbook, load the matching folder under `skills/` and then the playbook file it names. Do not load every playbook into context.
